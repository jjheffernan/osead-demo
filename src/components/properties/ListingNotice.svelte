<aside class="listing-notice" aria-label="Housing and demo notice">
	<p>
		<strong>Equal Housing Opportunity.</strong> O-sea-D supports fair housing and
		does not discriminate in housing-related services.
	</p>
	<p>
		<strong>Demo inventory.</strong> Sample listings only — not live offers or a
		real brokerage transaction. We typically reply within one business day.
	</p>
</aside>

<style>
	.listing-notice {
		margin: var(--space-md) 0 0;
		padding: var(--space-sm) var(--space-md);
		border: 1px solid var(--color-border);
		background: var(--color-bg-secondary);
		color: var(--color-text-secondary);
		font-size: var(--text-sm);
		line-height: var(--leading-relaxed);
	}

	.listing-notice p {
		margin: 0;
	}

	.listing-notice p + p {
		margin-top: var(--space-xs);
	}

	.listing-notice strong {
		color: var(--color-text-primary);
	}
</style>
